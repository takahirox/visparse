Independent fixture oracle — authored before the cross-site adapter execution.
Author: repository maintainer's implementation agent; synthetic benchmark, not real Kirka/Kraft behavior.
Reset: fresh unauthenticated context per viewport/run; saved selections empty. Reload retains context.
Recorded source path: save item 1, reload, remove item 1. Other source components: tab selection; modal closes with Escape and returns focus; invalid form blocked, corrected form submits, cancellation clears input; filter preserves catalog; async request first fails, retry succeeds. Clicking already-focused no-op has no DOM change. Missing selector is collection failure.
Target catalog: a1 Growing a city garden, a2 A guide to bicycles. Save/remove semantics match; retain titles/IDs and unrelated filter/detail/form controls. Store both item IDs independently, preserve a2 when a1 is removed. Reload persistence is same-context only. Do not infer cross-device/server persistence.
Held-out target paths: save both, remove first, reload retains second; cancel details with Escape returns focus; invalid form cannot submit, correction submits, cancel clears; retry after observed failure succeeds; filtering does not alter saved set.
Behavior and target preservation are separate cases. Appearance compares target catalog layout before/after transfer in the same state, not a claimed match to source screenshots.
Protocol v2 review correction before v2 execution: two deterministic external adapter executions per viewport (800x600 and 390x844), four total. No live-model scoring. Stored analysis ablations: full screenshot/action/DOM/AX and screenshots-only, two runs per viewport each; video-actions explicitly unsupported. Pin exported inputs and adapter bytes by SHA-256. Generator gets only neutral export/handoff and target HTML/context. Oracle and source code/images are absent from its input directory.
